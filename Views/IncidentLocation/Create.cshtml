@model GestionLocationWebApplication.Models.Entities.IncidentLocation

@{
    ViewData["Title"] = "Create";
    //Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "_Layout7";
}

<h1>Create</h1>

<h4>IncidentLocation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Incident" class="control-label"></label>
                <select  asp-for="Incident" class="form-control" asp-items="ViewBag.Incident">
                    <option value="">-- Liste des incidents --</option>
                </select>
                <span asp-validation-for="Incident" class="text-danger"></span>
            </div>
            
            <div class="form-group">
                <label asp-for="Location" class="control-label"></label>
                <select  asp-for="Location" class="form-control" asp-items="ViewBag.Location">
                    <option value="">-- Liste des locations --</option>
                </select>
                <span asp-validation-for="Location" class="text-danger"></span>
            </div>
            
            <div class="form-group">
                <label asp-for="Achat" class="control-label"></label>
                <select  asp-for="Achat" class="form-control" asp-items="ViewBag.Achat">
                    <option value="">-- Liste des achats --</option>
                </select>
                <span asp-validation-for="Achat" class="text-danger"></span>
            </div>
            
            <div class="form-group">
                <label asp-for="TypeIncident" class="control-label"></label>
                <input asp-for="TypeIncident" class="form-control" />
                <span asp-validation-for="TypeIncident" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts
{
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}